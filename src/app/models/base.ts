export interface Customized {
  style?: React.CSSProperties;
  className?: string;
}

export enum IconType {
  laptop = "laptop",
  award = "award",
  copilot = "copilot",
  translate = "translate",
  handshake = "handshake",
}
