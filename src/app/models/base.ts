export interface Customized {
  style?: string;
  className?: string;
}

export enum IconType {
  laptop = "laptop",
  award = "award",
  copilot = "copilot",
  translate = "translate",
  handshake = "handshake",
}
